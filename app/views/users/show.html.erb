<!DOCTYPE html>
<html>
<head>
  <title>User page</title>
</head>
<body>
  <div class ="container">
    <div class="row justify-content-center">
      <div class="col-4 mb-3"> 
      <% background_cards = ["pastel-blue", "pastel-pink", "pastel-green", "pastel-yellow", "pastel-orange", "pastel-purple"] %>
        <div class="card <%= background_cards.sample %>" style="width: 18rem;">
      <div class="card-body">
      <h5 class="card-title"><%= @user.first_name %> <%= @user.last_name %></h5>
      <p class="card-text"><%= @user.description %></p>
      <p class="card-text"><%= @user.email %></br><%= @user.age %> ans </br><%= link_to @user.city.name, city_path %></p>
      
      </div>
    </div>
    </div>
    </div>
  </div>
</body>
