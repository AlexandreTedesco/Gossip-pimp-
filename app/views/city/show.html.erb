<!DOCTYPE html>
<html>
<head>
  <title>Welcome page</title>
</head>
<body>
  <div class="container">
    <div class="navbar" style="width: 100%;">
      <div class="card-body">
      <nav class="navbar rounded navbar-expand-lg navbar-dark bg-dark">
    <span class="navbar-brand mb-0 h1"><%= link_to "THE GOSSIP PROJECT", home_path, class: "navbar-brand" %></span>
    <%= link_to "Créer un gossip", new_gossip_path, class: "navbar-brand" %>
    <%= link_to "Contacter nous", contact_path, class: "navbar-brand" %>
    <%= link_to "Notre équipe", team_path, class: "navbar-brand" %>
    </nav>
  </div>
  </div>
  </div>
  <div class="container">
    <div class="container">
      <h1>Ville : <%= @city.name %></h1>

      <h2>Potins écrits par des utilisateurs de : <%= @city.name %></h2>
      <% if @gossips.any? %>
    <ul>
      <% @gossips.each do |gossip| %>
      <li>
        <%= gossip.title%> écrit par <%= gossip.user.first_name %> -
        <%= gossip.content %>
      </li>
    <% end %>
    </ul>
    <% else %>
      <p>Aucun gossip disponible pour cette ville.</p>
    <% end %>
  </div>
  </div>
</body>